# Minimal dependencies to boot the Upwork Jobs Database API (backend/api/database.py)
# in the Alloy sandbox. The full project (selenium, pandas, etc.) is only needed
# for scraping/analysis, not for serving the REST API + Swagger UI.
fastapi>=0.100
uvicorn[standard]>=0.22
pydantic>=2.0
